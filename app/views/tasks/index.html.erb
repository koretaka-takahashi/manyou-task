<%= notice %>

<h1><%= t('view.index')だよ %></h1>

<table>
  <% @tasks.each do |t| %>
    <tr>
      <th><%= link_to "<#{t.name}>", task_path(t.id) %></th>
      <td><%= t.content %></td>
      <td><%= link_to t('view.edit'), edit_task_path(t.id) %></td>
      <td><%= link_to t('view.delete'), task_path(t.id), method: :delete, data: { confirm: t('view.confirm') } %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to t('view.new'), new_task_path %></p>
